# Sprite assets for Babylon Footbridge Lab

This folder is reserved for future game sprite assets.

Expected future sprite sheet:

```text
/sprites/learner-sprite-sheet.png
```

Recommended frame groups:

```text
idle: 4 frames
walk: 4 frames
climb: 4 frames
descend: 4 frames
celebrate: 4 frames
```

The current Babylon lab uses a generated fallback sprite drawn in code, so the experiment can run before a final artist-made sprite sheet is uploaded.
